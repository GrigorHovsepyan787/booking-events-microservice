<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd"
        objectQuotingStrategy="QUOTE_ONLY_RESERVED_WORDS">
    <changeSet id="1780657204216-1" author="grigor">
        <createTable tableName="events">
            <column autoIncrement="true" name="id" type="BIGINT">
                <constraints nullable="false" primaryKey="true" primaryKeyName="pk_event"/>
            </column>
            <column name="title" type="VARCHAR(255)"/>
            <column name="description" type="VARCHAR(255)"/>
            <column name="event_date" type="DATETIME"/>
            <column name="location" type="VARCHAR(255)"/>
            <column name="capacity" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="seats_available" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT"/>
            <column name="created_at" type="DATETIME">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="DATETIME">
                <constraints nullable="false"/>
            </column>
            <column name="version" type="BIGINT" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex
                tableName="events"
                indexName="idx_events_user_id">
            <column name="user_id"/>
        </createIndex>

        <sql>
            ALTER TABLE events
                ADD CONSTRAINT chk_capacity_positive
                    CHECK (capacity > 0);
        </sql>

        <sql>
            ALTER TABLE events
                ADD CONSTRAINT chk_seats_available_positive
                    CHECK (seats_available >= 0);
        </sql>

        <sql>
            <![CDATA[
            ALTER TABLE events
                ADD CONSTRAINT chk_seats_capacity
                    CHECK (seats_available <= capacity);
            ]]>
        </sql>

        <rollback>
            <sql>
                ALTER TABLE events DROP CONSTRAINT chk_seats_capacity;
                ALTER TABLE events DROP CONSTRAINT chk_seats_available_positive;
                ALTER TABLE events DROP CONSTRAINT chk_capacity_positive;
            </sql>

            <dropUniqueConstraint tableName="events" constraintName="idx_events_user_id"/>
            <dropTable tableName="events"/>
        </rollback>
    </changeSet>

</databaseChangeLog>